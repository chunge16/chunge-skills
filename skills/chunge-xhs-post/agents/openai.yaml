interface:
  display_name: "Chunge XHS Post"
  short_description: "一键协调小红书图片与文案生成的整套发布工作流"
  default_prompt: "Use $chunge-xhs-post to create a full Xiaohongshu post package with both image direction and publish-ready copy unless I explicitly ask for text-only or image-only."
